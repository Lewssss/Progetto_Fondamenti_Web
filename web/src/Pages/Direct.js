import React, { useState } from "react";
import Directfunction from "./Direct";
import "./Direct.css";
// Importa il file CSS per gli stili

//Usa grid coso e metti la foto

function Direct({ name, onBack, chatId, userId }) {}

export default Direct;
